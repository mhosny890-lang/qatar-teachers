'use client';
import {useState} from 'react';import {supabase} from '../../../lib/supabase';import {useRouter} from 'next/navigation';
export default function Page(){const [msg,setMsg]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const {data,error}=await supabase.rpc('admin_login',{p_username:String(f.get('username')),p_password:String(f.get('password'))});setLoading(false);if(error||!data?.length){setMsg('اسم المستخدم أو كلمة المرور غير صحيحة');return;}localStorage.setItem('qt_admin_token',data[0].token);localStorage.setItem('qt_admin_role',data[0].role);localStorage.setItem('qt_admin_name',data[0].full_name);router.push('/admin/dashboard')}
return <main className="section"><div className="container"><form onSubmit={submit} className="panel" style={{maxWidth:480,margin:'60px auto'}}><div className="brandmark" style={{margin:'auto'}}>Q</div><h2>Qatar Teachers Admin</h2><p>دخول الإدارة والمديرين والمشرفين</p><input name="username" required style={{width:'100%',padding:13,margin:'7px 0'}} defaultValue="hosn890"/><input name="password" required style={{width:'100%',padding:13,margin:'7px 0'}} type="password" placeholder="كلمة المرور"/><button disabled={loading} className="primary" style={{display:'block',width:'100%',marginTop:12}}>{loading?'جاري الدخول...':'دخول لوحة الإدارة'}</button>{msg&&<p>{msg}</p>}</form></div></main>}
