'use client';import {useEffect,useState} from 'react';import {supabase} from '../../lib/supabase';import {useRouter} from 'next/navigation';export default function Page(){const r=useRouter();const [center,setCenter]=useState<any>(null);const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user){r.push('/login');return}const {data:p}=await supabase.from('profiles').select('id,role,status,full_name,email,phone').eq('auth_user_id',user.id).single();if(!p||p.role!=='center'){r.push('/login');return}const {data:c}=await supabase.from('centers').select('*').eq('profile_id',p.id).single();setCenter({...c,profile:p});setLoading(false)})()},[r]);if(loading)return <main className="section"><div className="container"><div className="panel">جاري تحميل لوحة المركز...</div></div></main>;return <main style={{minHeight:'100vh',background:'#f7f7f9',padding:'30px'}} dir="rtl"><div className="container"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:15,flexWrap:'wrap'}}><div><h1 style={{color:'#8A1538',marginBottom:6}}>لوحة المركز التعليمي</h1><p>{center?.name_ar||center?.profile?.full_name}</p></div><button className="ghost" onClick={async()=>{await supabase.auth.signOut();r.push('/login')}}>تسجيل الخروج</button></div><div className="triple"><div className="panel"><h2>{center?.approved?'معتمد':'قيد المراجعة'}</h2><p>حالة الحساب</p></div><div className="panel"><h2>{center?.active?'نشط':'موقوف'}</h2><p>الحالة</p></div><div className="panel"><h2>{center?.is_featured?'مميز':'عادي'}</h2><p>الظهور</p></div></div><div className="dual" style={{marginTop:25}}><div className="panel" style={{textAlign:'right'}}><h2>بيانات المركز</h2><p><b>الموقع:</b> {center?.location||'-'}</p><p><b>المدينة:</b> {center?.city||'-'}</p><p><b>الهاتف:</b> {center?.phone||'-'}</p><p><b>واتساب:</b> {center?.whatsapp||'-'}</p><p><b>سعر الفردي:</b> {center?.individual_price||'-'} ر.ق</p><p><b>سعر المجموعة:</b> {center?.group_price||'-'} ر.ق</p></div><div className="panel"><h2>إدارة الحساب</h2><p>تعديل الملف، الصور، المواد، الأسعار والموقع سيتم من هنا.</p><a className="primary" href="/centers">عرض صفحة المراكز</a></div></div></div></main>}
